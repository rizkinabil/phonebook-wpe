import { H1, Space } from 'cherry-components';

const Header = () => {
  return (
    <>
      <Space size={20} />
      <H1>PhoneBook</H1>
      <Space size={20} />
    </>
  );
};

export default Header;
